package app

// Reserved for app-wide bootstrap helpers. The historic auto-admin bootstrap
// was removed: authentication is now opt-in and the first admin is created
// through the in-app setup wizard.
