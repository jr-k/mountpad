tmp/
bin/
out/
*.db
*.db-wal
*.db-shm
.env
.env.local

frontend/dist
frontend/node_modules
node_modules

# Stale tsc artefacts (we set noEmit:true but defense in depth -
# Vite resolves *.js BEFORE *.tsx, so a leftover compiled file
# would shadow the source and load stale code at runtime).
frontend/tsconfig.tsbuildinfo
frontend/src/**/*.js
frontend/src/**/*.js.map
frontend/src/**/*.d.ts.map
!frontend/src/styled.d.ts
!frontend/src/vite-env.d.ts

examples/storage/
!examples/.gitkeep

# Host bind-mount roots created by `docker compose up` at the repo
# root. Keep them ignored so a fresh checkout never carries stale
# DB files or user mount content from the previous developer.
data/
storage/

.DS_Store
*.log
